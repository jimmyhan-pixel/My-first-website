# My-first-website
Practice to build a website

## Fixes applied

- Removed accidental Markdown code fences from `script.js` and `style.css` so the browser can parse them.
- Added `id="chatMessages"` to the chat messages container in `index.html` so the chat script can find and append messages.

To preview locally run:

```bash
cd "/Users/hh/Documents/AI study/Githhub/My-first-website"
python3 -m http.server 8000
# then open http://localhost:8000 in your browser
```
